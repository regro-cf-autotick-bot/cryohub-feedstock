{% set name = "cryohub" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/cryohub-{{ version }}.tar.gz
  sha256: e51c293aa86c694aa0794175256f2e5d4909af3709c303984536b59b65cf0e43

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
  run:
    - click
    - cryotypes >=0.2.0
    - dask-core
    - dynamotable >=0.2.4
    - emfile >=0.3
    - h5py
    - ipython
    - mrcfile >=1.3.0
    - numpy
    - pandas
    # - pydantic  # Declared but not imported in source
    - python >=3.8
    - scipy
    - starfile >=0.4.5

test:
  imports:
    - cryohub
  commands:
    - pip list
    # - pip check  # Fails due to pydantic
  requires:
    - pip

about:
  home: https://pypi.org/project/cryohub
  summary: IO hub for Cryo-EM, Cryo-ET and subtomogram averaging data.
  license: GPL-3.0-only
  license_file: LICENSE
  dev_url: https://github.com/teamtomo/cryohub

extra:
  recipe-maintainers:
    - alisterburt
    - brisvag
    - goanpeca
